# PaperPlane Environment Configuration

# =============================================================================
# Admin Authentication (required for production)
# =============================================================================
# Set a strong secret token to protect write endpoints (scrape, apply, etc.)
# Without this, all endpoints are open (dev mode)
ADMIN_TOKEN=

# =============================================================================
# LLM Configuration
# =============================================================================
# Get your Gemini API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# Notification Configuration (choose one or more)
# =============================================================================

# Discord Webhook URL (create in Server Settings > Integrations > Webhooks)
DISCORD_WEBHOOK_URL=

# Telegram Bot Token (create via @BotFather)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ntfy.sh topic (free, no signup - just pick a unique topic name)
NTFY_TOPIC=paperplane-your-unique-id

# Email SMTP (for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFICATION_EMAIL=

# =============================================================================
# Browser Configuration
# =============================================================================
# Set to 'true' to see the browser while it works (useful for debugging)
HEADLESS=true

# LinkedIn session cookies (optional - for LinkedIn integration)
# Export from browser after logging in
LINKEDIN_LI_AT=
LINKEDIN_JSESSIONID=

# =============================================================================
# Application Settings
# =============================================================================
# Maximum applications per run
MAX_APPLICATIONS_PER_RUN=10

# Delay between applications (seconds) - helps avoid detection
APPLICATION_DELAY_MIN=30
APPLICATION_DELAY_MAX=120

# Auto-submit applications (false = review mode, true = auto-submit)
AUTO_SUBMIT=false

# =============================================================================
# Job Scraper Configuration
# =============================================================================

# Glassdoor session cookies (for Google OAuth users)
# How to get: Login to Glassdoor in your browser (can use Google Sign-in),
# then export cookies using an extension like "Cookie-Editor".
# Copy the full cookie string (e.g., "name1=value1; name2=value2")
GLASSDOOR_COOKIES=
# OR just the GSESSIONID cookie value:
GLASSDOOR_GSID=

# Note: Careerjet, Google Jobs, Greenhouse Jobs, and Levels.fyi 
# do NOT require any configuration - they work out of the box!

# =============================================================================
# Cold Email Configuration (Optional)
# =============================================================================

# Apollo.io session cookies (for contact scraping)
# How to get: Login to Apollo.io in your browser (can use Google Sign-in),
# then export cookies using an extension like "Cookie-Editor".
# Copy the full cookie string (e.g., "name1=value1; name2=value2")
# Apollo.io API Key (recommended) - More reliable than cookies
APOLLO_COOKIES=
# Apollo.io Session Cookies (fallback) - Copy from browser after logging in
APOLLO_API_KEY=  # Get from https://app.apollo.io/#/settings/integrations/api

# Cold email sending limits
COLD_EMAIL_DAILY_LIMIT=50
COLD_EMAIL_DELAY_SECONDS=120

# Note: SMTP settings above are used for sending cold emails.
# Make sure SMTP_USER and SMTP_PASSWORD are configured!

